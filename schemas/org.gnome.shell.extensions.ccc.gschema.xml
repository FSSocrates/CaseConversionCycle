```xml
<?xml version="1.0" encoding="UTF-8"?>
<schemalist>

  <schema
      id="org.gnome.shell.extensions.ccc"
      path="/org/gnome/shell/extensions/ccc/">

    <key name="toggle-hotkey" type="as">
      <default>['F1']</default>
      <summary>Case conversion hotkey</summary>
      <description>
        Global keyboard shortcut used to cycle selected text through
        UPPER CASE, lower case, and Title Case.
      </description>
    </key>

    <key name="selection-offset-apps" type="s">
      <default>''</default>
      <summary>Applications requiring a selection offset</summary>
      <description>
        One application identifier, window class, or process name per line.
      </description>
    </key>

  </schema>

</schemalist>
```
