<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.ccc" path="/org/gnome/shell/extensions/ccc/">
    <key name="toggle-hotkey" type="as">
      <default>['F1']</default>
      <summary>Case conversion hotkey</summary>
      <description>
        Global keyboard shortcut used to cycle the selected text through the case states.
        The default shortcut is F1 and can be changed in the extension preferences.
      </description>
    </key>

    <key name="selection-offset-apps" type="s">
      <default>''</default>
      <summary>Applications requiring a selection offset</summary>
      <description>
        One application ID, package name, or process name per line. These applications
        receive the two-character trailing-line offset instead of the normal one-character offset.
      </description>
    </key>
  </schema>
</schemalist>
